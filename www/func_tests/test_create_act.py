        # self.client = Client()
        # self.client.force_login(self.user)
        # cookie = self.client.cookies['sessionid']
        # self.driver.add_cookie({
            # 'name': 'sessionid',
            # 'value': cookie.value,
            # 'secure': False, 'path': '/'})
